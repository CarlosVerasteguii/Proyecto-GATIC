# --- OS / Editor ---
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
*.tmp

# --- IDEs / Local tooling (do not commit) ---
.vscode/
.idea/
.perf_*.txt
.perf_*.html
.perf_*.json
.perf_*.md

# Local-only study plan exports (not part of the app)
plan_estudios_qfb.png
plan_qfb.pdf

# Agent / assistant local state (do not commit)
.agent/
.claude/
.cursor/

# --- Company visual guide (keep local; avoid pushing corporate branding) ---
03-visual-style-guide.md

# --- BMAD output hygiene ---
_bmad-output/.archive/
_bmad-output/project-scan-report.json
_bmad-output/implementation-artifacts/validation-report-*.md

# Local helper output
git-status.txt
.perf_*

# --- Laravel / PHP (root-level, legacy) ---
.env
.env.*
!.env.example

/vendor/
composer.phar

storage/*.key
/public/storage
/public/hot
/public/build

.phpunit.result.cache
.phpunit.cache/
coverage/

# --- Node (Vite/Bootstrap, root-level) ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Laravel app in gatic/ subdirectory ---
# Environment files
gatic/.env
gatic/.env.*
!gatic/.env.example

# Dependencies
gatic/vendor/
gatic/node_modules/

# Build artifacts
gatic/public/build/
gatic/public/hot
gatic/public/storage

# Storage & cache
gatic/storage/*.key
gatic/bootstrap/cache/*
!gatic/bootstrap/cache/.gitkeep

# Testing
gatic/.phpunit.result.cache
gatic/.phpunit.cache/
gatic/coverage/
gatic/.pest/
Context Chats

# AI Tools
.agents/
.codex/
.gemini/
.gemini-clipboard/
.qoder/
.github/skills/
